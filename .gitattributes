# Normalisation des fins de ligne : évite que tout le dépôt apparaisse
# comme modifié selon la machine (Windows CRLF vs Linux LF).
* text=auto eol=lf

# Fichiers binaires : jamais de conversion
*.png binary
*.jpg binary
*.ico binary
*.zip binary
